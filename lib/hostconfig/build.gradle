plugins {
    alias(libs.plugins.android.lib)
}

android {
    namespace = "com.example.hostconfig"

    buildFeatures {
        buildConfig = true
    }

    defaultConfig {
        buildConfigField("String", "HOST", "String.valueOf(\"https://api.example.com\")")
    }
}

apply(from: "load1.gradle.kts")
apply(from: "load2.gradle")

tasks.register("dummyTask") {
    doLast {
	println("This is a dummy task.")
    }
}
