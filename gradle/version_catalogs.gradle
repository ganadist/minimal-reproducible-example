enableFeaturePreview("VERSION_CATALOGS")
dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("versions/libs.toml"))
	    // load versions from gradle.properties
            version("r8", r8Version)
            version("android-gradle", androidGradlePluginVersion)
	    version("ksp", kspPluginVersion)
        }
        androidx {
            from(files("versions/androidx.toml"))
        }
        google {
            from(files("versions/google.toml"))
        }
    }
}

