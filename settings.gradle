pluginManagement {
    repositories {
	gradlePluginPortal()
	google()
        mavenCentral()
    }
    plugins {
        id 'com.google.devtools.ksp' version kspPluginVersion
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
	google()
        mavenCentral()
    }
}

apply from: "gradle/version_catalogs.gradle"

include ':app', ':dynamicfeature1', ":shared", ":lib:glide"
rootProject.name='My Application'
